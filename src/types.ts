export enum Color {
  DEFAULT = 'default',
  PRIMARY = 'primary',
  SECONDARY = 'secondary'
}

export enum Variants {
  CONTAINED = 'contained',
  OUTLINED = 'outlined',
  TEXT = 'text',
  ICON = 'icon'
}

export enum Size {
  SMALL = 'small',
  MEDIUM = 'medium',
  LARGE = 'large'
}
